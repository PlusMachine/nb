# Сквозной UX-проход 2026-07-15: отчёт и план работ

**Статус: находки зафиксированы, фиксы НЕ делались.**

Как проверялось: живой прогон в Playwright под обычным пользователем (qa.user, роль user, сидовый склад), два полных флоу:

1. **Чужой рецепт**: витрина «Рецепты сообщества» → поиск → Pilsner Urquell → «Скопировать себе» → адаптация под своё оборудование (профиль Grainfather G30, пересчёт под 22 л) → мастер воды (осмос → профиль «Пльзень», соли + молочная кислота, pH 5.35) → «Сварить» → ветка автоматики (демо-пивоварня) → брожение (OG/FG замеры) → «Завершена».
2. **Свой рецепт с нуля**: «Цитровый IPA» 21A → ингредиенты через пикер (склад/каталог, инкрементальный ввод по 2–3 символа) → «Сварить самому» → списание со склада → гид варочного дня (9 шагов) → «Поставить на брожение».

Оба флоу проходимы до конца. Ядро (расчёты, автосейв, списание, гид) работает. Ниже — находки в порядке приоритета, с якорями на код и приёмками.

Пересечение с параллельной работой: идёт незакоммиченный редизайн публичной страницы рецепта (полоса показателей с треками стиля). Ф1 и Ф6 трогают ту же страницу — синхронизировать перед началом.

---

## P0 — доверие к данным и блокирующие дефекты

- [x] **Ф1. Дрейф показателей при копировании рецепта.** — СДЕЛАНО 2026-07-15. Разведка опровергла гипотезу «дефолтная эффективность»: параметры партии копия наследовала и раньше. Реальные причины: (1) у витринных рецептов сохранённые статы — авторитетные цифры первоисточника поверх движка, а копия всегда пересчитывалась движком; (2) редактор молча обнулял унаследованный снапшот оборудования, если профиль принадлежит автору оригинала. Фикс: копия наследует og/fg/abv/ibu/color источника (кроме явного пересчёта под другой объём); чужой equipmentProfileId в копию не переносится, снапшот сохраняется и редактором не выбрасывается; селект показывает «Оборудование автора рецепта»; в редакторе закрываемая плашка «Показатели автора отличаются от расчёта движка: …» по разошедшимся метрикам. Тесты: clone-service, lifecycle (5 сквозных), designer-equipment-snapshot (новый файл); живой прогон на dev-БД: копия PU = 1.048/4.4/40/4 как у источника.
  Витрина: OG 11.9 °P / ABV 4.4% / IBU 40 / SRM 4 («В стиле»). Сразу после «Скопировать себе»: НП 13.1 °P / ABV 5.8% / IBU 42 / SRM 5.8 (ABV «Выше») — молча. Причина: публичная страница показывает сохранённые статы автора, а редактор пересчитывает копию своим движком с дефолтной эффективностью (72%), не унаследовав параметры оригинала.
  Где: `apps/web/features/recipes/service.ts` (~1963, билдер payload клона — какие параметры партии переносим), `components/recipes/recipe-designer/*` (пересчёт при открытии).
  Варианты: (а) копия наследует эффективность/объём/оборудование-параметры оригинала, пока пользователь сам не сменит профиль; (б) если пересчитали — явный баннер «Показатели пересчитаны под ваши настройки: ABV 4.4% → 5.8%» с кнопкой «вернуть параметры автора».
  Приёмка: копия PU сразу после создания показывает OG/ABV/IBU/SRM оригинала (или явное объяснение разницы); тест на копирование со сравнением статов.

- [x] **Ф2. «Совпадение со складом» противоречит списанию.** — СДЕЛАНО 2026-07-15 (одной волной с П3). Причина: два независимых движка — discovery-матч прощает бренд по groupKey (`fermentable:pilsner`), а списание матчило строго по `ingredientCatalogItemId`. Фикс: «Списать со склада» всегда открывает диалог-предпросмотр (`previewBrewBatchConsumption`): точные позиции, замены по match-group (opt-in чекбокс «вместо X», сравнение «EBC 5.3 ↔ 4.8»/«α …», select при нескольких кандидатах), нехватки — ссылки «Найти в каталоге»/«Чего не хватает». Сервер валидирует каждую замену пересчётом кандидатов (подделка → INVALID_SUBSTITUTION, откат всего); превью использует тот же подбор лота (`inventorySelectionMeta`) и масштаб партии, что и списание; «вместо X» видно в списанном (мета аллокации/транзакции). exact_short делится по `exactClamps`: дрожжи — «спишем остаток» (кламп), прочие — «не хватит» + предложение замены + блок подтверждения; клиентский гард двойного бронирования одной позиции двумя строками. «Сварить самому» остаётся exact-only, но тост честно сообщает про доступные замены (`consumeSubs=1`; после частичного списания — через «верните списание и спишите заново», т.к. списание одноразовое). Тесты: recipe-match-service (кандидаты замен), brew-batch-consume-substitution (превью/замена/валидация/лот), consume-preview-dialog (виды строк, блок-гарды); живой прогон на dev-БД: замена предложена и списана (5000→3000 г, «вместо» на месте), возврат восстановил, подделка отбита без побочек.
  Публичная страница PU: «Есть 1 из 8» (match-group: курский пилс ≈ Beerex пилснер). Партия того же рецепта, «Списать со склада»: «На складе нет сопоставленных позиций для списания» (точное совпадение ingredientId). Для пользователя это взаимоисключающие утверждения. Плюс тупик: ни списка «чего не хватает», ни ссылки на «Чего не хватает»/каталог.
  Где: `apps/web/app/(app)/app/brew-batches/[id]/actions.ts` (списание), `components/recipes/recipe-match-panel.tsx` (матч по группам).
  Решение: списание должно предлагать замены по match-group (как match), с явным подтверждением «списать Курский пилс вместо Beerex?»; в пустом случае — CTA на «Чего не хватает».
  Приёмка: у qa.admin (пилс на складе, рецепт PU) списание предлагает замену; сообщение-тупик заменено на actionable.

- [x] **Ф3. Лексика завершения партии + вердикт брожения.** — СДЕЛАНО 2026-07-15. (а) Переименована тройка действий + итог (объём согласован с владельцем): «Завершить партию» (кнопка + заголовок диалога), «Отменить партию» (пункт меню + confirm-диалог, включая тело «Партия будет помечена отменённой»), «Действия с партией» (aria-label меню), «Итог партии» (карточка сводки). «Варка завершена» (стадия BrewForge) и «…для следующей варки» (сеанс ареометра) не тронуты — там «варка» в правильном значении. (б+в) В `verdict-core` добавлены два приоритетных внешних факта (опциональные поля входа, проверяются до эвристики и до минимума точек): `batchCompleted` → **batch_completed** «Брожение завершено», иначе `fgConfirmed` (есть ручной замер isFinal) → **fg_confirmed** «Добродило — FG зафиксирован»; `series.ts` вычисляет оба из `batch.status`/`manualMeasurements` и пробрасывает; на отменённой партии строка вердикта скрыта вовсе; `alerts.ts` (пуши по данным устройств) не менялся — флаги там не передаются, поведение бит-в-бит. Тесты: verdict-core (+7 кейсов приоритета/регрессии), новый `series.test.ts` (проводка через мок @nb/db), обновлены brew-completion/brew-batches-page-wiring; живой прогон на dev-БД: active → fg_confirmed → batch_completed, 10/10 PASS. Хвосты (не брались, кандидаты в следующие волны): вердикт якорится на последней точке ряда и может залипнуть «Бродит активно» на брошенной (но не завершённой) партии; завершение партии не закрывает сеанс ареометра; пуши не знают статус партии (возможен пуш по completed); галка FG доступна и на OG-акте.
  а) На этапе «Брожение» кнопка называется «Завершить варку», хотя завершает партию (текст диалога сам говорит «Партия перейдёт в „Завершена“»). Переименовать в «Завершить партию» (кнопку и диалог).
  б) На завершённой партии блок брожения продолжает показывать «Бродит активно».
  в) Галка «Это финальный замер (FG)» не влияет на вердикт: после FG-замера статус остаётся «Бродит активно».
  Где: `features/brew-batches/components/fermentation-board.tsx`, `features/brew-batches/brew-day.ts`, `features/device-streams/verdict-core.ts`, `features/device-streams/components/batch-ferment-block.tsx`.
  Приёмка: на этапе брожения кнопка «Завершить партию»; batch stage=completed ⇒ вердикт-блок показывает итог («Брожение завершено», не «активно»); FG-замер переводит вердикт в «добродило/стабильно».

- [x] **Ф4. Залипание `pointer-events: none` на body после «Изменить этап…».** — СДЕЛАНО 2026-07-15 (7235c59). Причина найдена на уровне кода Radix: и DropdownMenu, и Dialog — модальные слои с общей module-level переменной `originalBodyPointerEvents` в `@radix-ui/react-dismissable-layer`; синхронное открытие диалога из onSelect пункта меню кладёт размонтирование меню и монтирование диалога в один React-коммит, при неудачном порядке эффектов «none» оседает как «оригинальное» значение и восстанавливается навсегда при финальном закрытии диалога (потому «1 раз из 2» и лечится только перезагрузкой). Фикс паттерном: в `@nb/ui` dropdown-menu вызов `item.onSelect` отложен на тик (`setTimeout 0`) — закрыл все 4 уязвимых места (batch-menu, owner-recipe-card, admin-recipes-list, admin-catalog-list). Приёмка выполнена как e2e (в vitest/node Portal не рендерится): `e2e/batch-menu-pointer-events.spec.ts`, 10 циклов смены этапа; тест верифицирован откатом фикса — без него падает на 1-м цикле.
  Наблюдал вживую один раз: после выбора этапа в диалоге body остался с inline `pointer-events: none` при нуле открытых диалогов — вся страница мертва для мыши (клавиатура работает), лечится только перезагрузкой. Похоже на незачищенный scroll/pointer-лок Radix при «выбор → закрытие диалога → router.refresh». Чистое репро поймать не удалось (один раз из двух).
  Где: `features/brew-batches/components/batch-menu.tsx` (диалог «Изменить этап»), проверить паттерн «закрываем Dialog программно одновременно с навигацией/refresh».
  Приёмка: 10 циклов смены этапа подряд — body без inline pointer-events; e2e-проверка кликабельности после смены этапа.

- [x] **Ф5. Плавающая кнопка «Обратная связь» перекрывает элементы форм.** — СДЕЛАНО 2026-07-15 (7b4696c). Причина структурная: в «реестре высот нижнего хрома» (cookie-баннер/нижняя навигация/sticky-бар пишут CSS-переменные) FAB был единственным, кто чужие переменные читал, а свою не публиковал; на десктопе контенту резервировалось 40px при следе кнопки ~58px. Фикс по выбору владельца (резерв, текст кнопки на десктопе сохранён): FAB публикует `--nb-fab-h` (ResizeObserver, паттерн cookie-баннера, сброс в 0px на анмаунт/админку/киоск), слагаемое добавлено в pb-формулы AppShell (обе ветки) и PublicShell + в bottom тост-вьюпорта (`@nb/ui` toast — бонус-находка: тосты вставали ровно поверх кнопки при большем z-index). e2e `feedback-fab-overlap.spec.ts` (1280/1440/1920 + сравнение резерва киоск/обычный), верифицирован откатом слагаемого.
  На 1440×900 FAB ложится ровно на «Добавить» в форме замера плотности (журнал замеров партии) — кнопка некликабельна на этой прокрутке. Ловил и на других страницах (перекрывала счётчик «Найдено N» и кнопки в подвале карточек).
  Где: `components/feedback/feedback-launcher.tsx`.
  Решение: либо прятать/сжимать FAB при пересечении с интерактивными элементами (уже есть переменная `--nb-cookie-banner-h` — аналогичный подход), либо поднять z-index форм ниже… правильнее — сместить FAB в самый угол и уменьшить, и дать формам отступ снизу на страницах с действиями в правом нижнем углу.
  Приёмка: на странице партии «Добавить» в форме замера кликается на вьюпортах 1280×800, 1440×900, 1920×1080.

---

## P1 — понятность ключевых развилок

- [x] **Ф6. «Сохранить» vs «Скопировать себе» на публичном рецепте — неразличимы.** — СДЕЛАНО 2026-07-15 (7228e5b, одной волной с П1+П2). Решение владельца: «В закладки»/«В закладках» + полный ребрендинг раздела «Сохранённые» → «Закладки» (пункт меню, /app/saved: metadata/h1/пустое состояние, тосты, aria-label иконок); автосейв-«Сохранить» редактора и кнопки форм не тронуты. Живой прогон: словарь на месте, следов «Сохранённых» в UI не осталось.
  Живой вопрос владельца с `/recipes/oatmeal-stout-stephen-galante`: «вижу сохранить и скопировать — чем отличается? что делает то, что другое?»
  Факт: «Сохранить» = закладка в таб «Сохранённые» (`components/recipes/recipe-save-button.tsx`), копию не создаёт, рецепт остаётся авторским. «Скопировать себе» = редактируемый черновик-копия в «Мои». Слово «Сохранить» без контекста читается как «сохранить себе» ≈ «скопировать» — оба глагола обещают одно и то же.
  Решение (обсудить формулировки с владельцем — правило «имена действиями»): «Сохранить» → **«В закладки»** (иконка Bookmark уже такая) или «В сохранённые»; у «Скопировать себе» оставить как есть. На витрине флажок-иконка — ок, там есть тултип-контекст.
  Приёмка: на детальной странице две кнопки читаются без наведения: закладка vs копия; тексты согласованы с владельцем.

- [x] **Ф7. Ветка «Сварить → Подключить BrewForge» теряет контекст варки.** — СДЕЛАНО 2026-07-15 (9a75188). Решение владельца: полный вариант — `?returnRecipe=<id>`, баннер «Устройство подключено» + «Продолжить варку „<название>“» с автооткрытием диалога (?brew=1, оба входа: редактор для своего, публичная страница для чужого); до подключения — строка «Подключите устройство или создайте демо-пивоварню, чтобы продолжить варку „<название>“». Чистое ядро в features/devices/return-recipe-core.ts (`kind==="brewforge" && status==="online"` — прокси «может варить», у DeviceTile нет supportsRecipePush). ⚠ P0 с ревью: чтение searchParams в page.tsx публичного рецепта динамизировало ВЕСЬ роут — ?brew=1 читается клиентом из window.location в эффекте (не useSearchParams — CSR-bailout), документ остался статическим. Живой прогон (qa.moderator без устройств): весь путь до автооткрытого диалога PASS, параметр вычищается, крестик стирает returnRecipe.
  Из диалога «Сварить рецепт» пункт «Подключить BrewForge» уводит на `/app/devices` без обратного пути: подключив устройство, пользователь должен сам вспомнить рецепт, найти его и нажать «Сварить» заново.
  Где: `components/recipes/brew-picker-dialog.tsx`, `app/(app)/app/devices/*`.
  Решение: передавать `?return=brew:<recipeId>` (или state) — после появления онлайн-устройства показать на «Устройствах» баннер «Устройство подключено → Продолжить варку „Цитровый IPA“».
  Приёмка: путь «Сварить → Подключить → (создать демо) → Продолжить варку» проходит без ручного поиска рецепта.

- [x] **Ф8. Поиск «Рецептов сообщества» не понимает транслит/раскладку.** — СДЕЛАНО 2026-07-15 (ba1658c). Решение владельца: переиспользуем машинерию @nb/brewing-core (варианты запроса + curated-словарь) + матч по стилю, раскладка — фолбэком при нуле результатов; «vayceh» из приёмки — иллюстрация, fuzzy не делаем. Реализация: resolveTextSearchScope в public-recipe-query.ts (порог уверенного стиля score 700 — константа CONFIDENT_STYLE_MATCH_SCORE, первое место для правки при ложных срабатываниях), searchPublicRecipes двухфазный. Фиксы ревью/прогона: гейт длины ≥3 на расширение вариантов И стили (регрессия: «и»→транслит «i» отдавал весь каталог 44/44 — поймано живым прогоном), пунктуационный q не отключает фильтр, единый кап 16 вариантов, экранирование %/_ в ilike. Живые числа: «пил»→3 (PU, Nevermore, Летний пилснер), «вайцен»→Weihenstephaner (по стилю 10A), «gbkcyth»→3 пилснера, «и»→12 честных substring.
  «пил» не находит Pilsner Urquell, «pilsner» не находит «Летний пилснер». При этом поиск стилей BJCP и поиск каталога ингредиентов кросс-раскладку умеют (там же логика «раскладка-фолбэк» уже есть — переиспользовать).
  Где: `components/recipes/recipes-toolbar.tsx` + серверный поиск витрины в `features/recipes/service.ts`.
  Приёмка: «пил» находит и «Летний пилснер», и Pilsner Urquell; «vayceh»/«вайцен» находят Weihenstephaner; тест на поиск.

- [x] **Ф9. Пересчёт под объём даёт «0.73 пачки» дрожжей.** — СДЕЛАНО 2026-07-15, дизайн владельца «граммы как факт» (СНГ-специфика: показывать реальную потребность, не молча округлять). Общий узел `scaleIngredientAmount` в `scale.ts` (оба движка): pack с известной граммовкой (мост `features/inventory/pack.ts`, дефолт 11 г для сухих) → конверсия в граммы («8.1 г»), скобка «(N пачек)» на публичной странице/в модалке пересчёта/в гиде варки (packEquivalent замораживается в снапшот, старые снапшоты рендерятся как раньше); pack без граммовки (жидкие) и item (шт.) → вверх до целого, мин. 1; в дизайнере «Пересчитать под N л» — ceil до целой пачки (technicalData туда не передаётся; конверсия единицы под ногами редактора спорна — если захочется, отдельным решением). Ревью-находка проверяющего (живой прогон): `applyCloneTargetVolume` мёржил только количество без единицы → клон сохранял «8.1 pack»; починено (мёрж всего ScaledAmount), тест в coverage-recipes-lifecycle верифицирован откатом. Живой прогон 10/10 PASS. Формат склада «1 пачка (11 г)» не тронут — там пачки и есть физический факт.
  После «Пересчитать ингредиенты под 22 л» дрожжи стали 0.73 пачки — так не отмеряют.
  Где: `components/recipes/recipe-designer/*` (пересчёт), `recipe-designer/helpers.ts`.
  Решение: для единицы «пачка» округлять вверх до целого (или до 0.5 с подсказкой), показывать хинт «нужно ≥ 0.73 пачки → 1 пачка». Граммы оставить точными.
  Приёмка: пересчёт 30→22 л для «1 пачка» даёт «1 пачка» (не 0.73); обратный пересчёт 22→44 л даёт «2 пачки».

- [x] **Ф10. План варочного дня дублирует засыпь.** — СДЕЛАНО 2026-07-15 (31d4b19). Парный предикат: в mash-цикле `brew-day.ts` затирочный солод (fermentable, `stepMeta.use` пустой/«mash») не порождает «Внести:» (он уже в «Засыпьте солод»); настойный (`use=steep`) и не-ферментируемые mash-добавки сохраняют шаги. Довесок (решение владельца «да, вместе»): симметричный фильтр в `brew-plan.ts` — сумма «Засыпьте солод» только из затирочного солода, кипятильная декстроза больше не раздувает вес (обновлены 2 теста, закреплявших старую семантику). Тесты: brew-day.test.ts +4, recipe-interop.test.ts (grainBillTotalKg на смешанном наборе), спот-чек прямым вызовом. Лега-си-риск: партии, созданные в окне 29fa120…69b6c1b (grainBillTotalKg=null в снапшоте), покажут dough-in без веса — не блокер.
  Для односолодового рецепта гид даёт «Засыпьте солод 5 кг» (до пауз) и отдельно «Внести: Пэйл Эль 5 кг» ПОСЛЕ часовой температурной паузы. Пивовар видит два внесения одного солода, второе — не в том месте.
  Где: `features/brew-batches/brew-day.ts:233` («Засыпьте солод»), `:284` («Внести: …» — тайминг-шаги ингредиентов).
  Решение: солод с использованием «Затор» не должен порождать отдельный шаг «Внести» (он уже входит в «Засыпьте солод»); отдельные шаги — только для внесений с явным «поздним» таймингом (настой, кипячение, брожение).
  Приёмка: односолодовый рецепт → в гиде одно внесение солода; рецепт с поздним внесением (например, декстроза в кипячение) сохраняет свой шаг.

- [x] **Ф11. Профиль оборудования нельзя создать из редактора рецепта.** — СДЕЛАНО 2026-07-15 (9a75188). «+ Создать профиль…» (сентинел в нативном select, значение откатывается) → НОВАЯ вкладка /app/equipment?mode=create — переход в той же вкладке молча терял бы последние ~1,5 с черновика (автосейв на дебаунсе, beforeunload ловит только закрытие вкладки). Возврат фокуса → router.refresh() (лимит 5 опросов на клик, дедуп focus/visibilitychange 1 с, ручной выбор снимает ожидание — фиксы ревью против вечного refresh и тихой подмены ручного выбора); появившийся ровно один новый профиль автовыбирается. Инлайн-диалог отвергнут: форма 17 полей + пресеты = «отдельная страница» по конвенции, а server action заканчивается redirect. Живой прогон: PASS, черновик цел.
  В дропдауне «Оборудование» только «Без профиля»/ручной ввод — пользователь должен догадаться пойти в «Оборудование», создать профиль и вернуться.
  Где: `components/recipes/recipe-designer/recipe-batch-parameters-block.tsx`.
  Решение: пункт «+ Создать профиль…» в конце списка → открывает `/app/equipment?mode=create` в новой вкладке или инлайн-диалог; после создания — профиль выбран.
  Приёмка: из редактора без профилей можно дойти до выбранного профиля не теряя черновик.

- [ ] **Ф12. Демо-пивоварня: варка выглядит сломанной.**
  а) Сценарий mash зациклен: таймер паузы скачет (осталось 6:00 → 37:30 → 22:30 → 40:30), график — пила из повторов затирания; варка не доходит до кипячения.
  б) «Далее» даёт тост «Стадия пропущена», но стадия/прогресс не меняются — я нажал трижды (на реальном железе так перескочат лишние стадии). Нужна мгновенная обратная связь: оптимистичная смена стадии или дизейбл кнопки до подтверждения устройства.
  в) Пульт показывает «Затирание (сценарий mash)», уставку 64 °C и «Пауза 1 из 2», хотя в рецепте один шаг 65 °C/60 мин — нигде не сказано, что демо крутит встроенный сценарий, а не рецепт. Подписать: «Демо-сценарий, не ваш рецепт» (или гнать демо по рецепту).
  Где: `packages/brewforge-sim/src/sim-device.ts` (луп сценария), пульт партии в `apps/web/app/(app)/app/brew-batches/[id]/*`.
  Приёмка: демо-варка проходит затор→кипячение→готово за ~10 мин без луп-повторов; «Далее» либо меняет стадию ≤2 с, либо блокируется с индикатором «команда отправлена».

---

## P2 — полировка и нейминг

- [ ] **Ф13. Разнобой десятичных разделителей на карточке рецепта**: «ABV 5,8 %» (запятая) рядом с «OG 13.1 °P» и «SRM 5.8» (точка). Выбрать один формат (владелец решает: RU-запятая везде или точка везде).
- [ ] **Ф14. Заголовок «Ваш рецепт и …»** в панели стиля — висящее «и» при переносе имени стиля на строку ниже («Ваш рецепт и \n BJCP Американский IPA»). Переверстать: «Рецепт vs стиль» / «Попадание в стиль» или неразрывный блок.
- [ ] **Ф15. ё/е вперемешку**: «Ещё»/«ОБЪЁМ, Л» в одних местах, «Еще параметры (опционально)», «Мертвый объем заторника», «3. Объем воды», «Расчет» — в других (профиль оборудования, мастер воды). Пройтись по формам, привести к ё.
- [ ] **Ф16. Пункт сайдбара «BrewForge» ведёт на страницу «Устройства»** — после M1–M5 там живут и RAPT, и ареометры. Переименовать пункт в «Устройства» (термин страницы уже принят).
- [ ] **Ф17. Сухое охмеление в чек-листе брожения**: «Внести на брожении: Цитра 60 г • 4 дн.» — формулировка корявая, и не сказано, в какой день вносить. Вариант: «Сухое охмеление: Цитра 60 г • держать 4 дн.» + день внесения, если задан.
- [ ] **Ф18. Формат даты mm/dd/yyyy** в «Дата варки» (нативный date-input). Проверить на реальной русской локали браузера; если и там m/d — заменить на локализованный пикер.
- [ ] **Ф19. Каталог: «Мозаик» (Beervingem) и «Мозаика» (YCH)** — один сорт, два русских написания. Свести к одному в каталожных данных.
- [ ] **Ф20. Графики брожения замусорены варочными данными.** «План vs факт» на этапе брожения строит «факт» из затирочных 60–70 °C (шкала до 78 °C, план 10 °C не читается); блок устройства пишет «Прибор больше не в режиме ферментации» (прибор в нём и не был). Фильтровать точки по началу этапа брожения; предупреждение переформулировать («Прибор не в режиме ферментации»).
- [ ] **Ф21. Списание при «Сварить самому» — есть, при автоматике — нет.** В диалоге ручной варки есть чекбокс «Списать ингредиенты со склада», в ветке автоматики списание доступно только потом со страницы партии. Выровнять: тот же чекбокс в подтверждении запуска нагрева.

---

## Дополнение 2026-07-15 (после волны 4): панель «Совпадение со складом» ↔ склад

Триггер — живой вопрос владельца на `/recipes/hefeweizen-jamil-zainasheff`: строка нехватки «Хефевайцен Эль» нечитаема (оказалось — дрожжи ASP Lab), а «На склад» из панели — «магия из инпута». Проверено разведкой по данным/UI и живым осмотром под qa.admin (скриншоты пути — scratchpad сессии, `shots-magic/*`).

**Главный ответ: движок ничего не подменял.** ASP Lab прописан в сиде руками: `seed-public-recipes.ts:195` — `C.hefe = "asp-lab-al-205-hefeweizen-ale"`, рецепт Джамиля собран как `y(C.hefe)` (:488). Матч/списание работают строго по `ingredientCatalogItemId`, фаззи-подбора нет. Это ошибка данных сида: брендово-верные позиции в каталоге уже есть и не использованы (`wyeast-3068-weihenstephan-weizen`, `white-labs-wlp300-hefeweizen-ale-yeast`).

Архитектурная заметка (не срочно, отдельное решение владельца): живое имя каталожной карточки перебивает снапшот строки рецепта (`features/recipes/service.ts:989`, `liveLinkage?.displayName ?? snapshot`) — переименование/перепривязка карточки каталога задним числом молча меняет текст опубликованных рецептов. Механизм намеренный (ренейм бренда долетает до рецептов), но означает: снапшот имя не защищает.

- [ ] **Ф22 (P1, данные). Сид-дрожжи ASP Lab в импортных рецептах.** Затронуты 4 константы → 7 строк в 6 рецептах: `hefe` (Джамиль Hefeweizen + Gordon Strong Weiss), `kolsch` (Джамиль Kölsch), `irish` (Guinness, Oatmeal Stout, Milk Stout — классические BYO/US-клоны), `wit` («Бланш де Луш», бир.рф — рецепт российский, ASP Lab там уместен, можно не трогать). Перепривязать на брендово-верные аналоги: hefe → `white-labs-wlp300-hefeweizen-ale-yeast` или `wyeast-3068-weihenstephan-weizen`; kolsch → `white-labs-wlp029-german-ale` (Wyeast 2565 в каталоге нет — завести отдельным тикетом?); irish → `wyeast-1084-irish-ale` (классика клонов Guinness) или `white-labs-wlp004-irish-ale`. Существующие строки БД обновить точечным UPDATE `recipe_ingredients` (id+snapshot): полный пересид снёс бы сохранения/оценки читателей на сид-рецептах. ⚠ У аналогов, как и у ASP-позиций, нет package_variants — хуже не станет; сверить размерность единиц (сейчас 35 мл). Профилактика: правило для новых сид-рецептов — бренд/страна дрожжей соответствует источнику рецепта. **Выбор аналогов (WLP vs Wyeast) и судьбу `wit` согласовать с владельцем.**
- [ ] **Ф23 (P1). Строки нехватки обезличены.** «Хефевайцен Эль» без категории и бренда — при том что тремя блоками выше, в составе рецепта, та же позиция показана полно («… • ASP Lab 🇷🇺, liquid, Атт. 74%, 20-22°C»). `line.category` уже в DTO (`RecipeMatchLineDto.category`) и просто не отрисован; бренда в DTO нет (но он уже в памяти match-service — join не нужен). Сделать: подпись вида «Дрожжи · ASP Lab» у строк «Не хватает на складе» и в развороте «Что есть и чего не хватает» (словарь `ingredientCategoryLabels` готов); имя строки — ссылка на деталку каталога (паттерн `catalogHref` уже работает на /app/shopping; в панели ссылки нет вовсе — проверено осмотром).
  Где: `recipe-match-panel.tsx`, `match-service.ts`, `contracts.ts` (аддитивное поле brand).
  Приёмка: по строке нехватки понятно, что это дрожжи и чей товар; клик по имени открывает деталку каталога.
- [ ] **Ф24 (P1). «На склад» вслепую — показать карточку ингредиента.** Сейчас инлайн-форма: имя + число (предзаполнено «23.333 мл») + «Добавить»; ни карточки, ни фасовки, ни подтверждения; тоста после добавления нет (единственная обратная связь — исчезновение строки и смена процента). При этом та же нехватка на /app/shopping уже ведёт в штатную модалку склада с полной карточкой (`buildIngredientCatalogActionHref` → `/app/ingredients?addSource=…` → `IngredientSelectionCard`: бренд, форма, Атт., температура, выбор фасовки, цена/даты). Развилка — **решить с владельцем ДО правок**:
  (А, дёшево, рекомендация разведки) заменить инлайн-форму этим готовым переходом в модалку склада; минус — уход со страницы рецепта, возврат к `#match-panel` — маленькая доработка (`?returnTo=`);
  (Б, дороже, ~1-2 дня) мини-карточка прямо в развороте строки: новый server action за `IngredientSuggestionItem`, лоадер; риск разрастись в мини-модалку — если делать, жёстко зафиксировать scope «карточка только для контекста».
  В любом варианте: тост после добавления (сейчас его нет).
  Приёмка: перед добавлением видно карточку товара (бренд/категория/форма); после добавления — явная обратная связь.
- [ ] **Ф25 (P2). Счётчик «не хватает N» расходится со списком.** Живой факт: шапка «Есть 2 из 4 · не хватает 1», а в «Не хватает на складе» — 2 строки. Счётчик = только `missing`, список `gaps` = `missing`+`partial` («Пшеничный солод — Частично 60%» в списке есть, в счётчике нет). Согласовать семантику: либо счётчик учитывает частичные, либо частичные строки несут статус-подпись и в свёрнутом списке (в развороте она уже есть).
  Где: `match-service.ts` (missingCount) / `recipe-match-panel.tsx` (gaps). Учесть: «не хватает N» теперь ссылка на /app/shopping (П2) и текст плашки П1 — менять согласованно.
- [ ] **Ф26 (P2). Предзаполненное количество — пересчёт, не упаковка.** В форме «На склад» подставляется 23.333 мл (нехватка 35 мл × 20/30 л) — дробное «под рецепт», тогда как жидкие дрожжи продаются фиалами/пакетами; штатная модалка склада, наоборот, оставляет количество пустым — два пути добавления ведут себя по-разному. Связать с тикетом граммовок пачек (волна 3, 0/349): при появлении package_variants предлагать целые упаковки; до тех пор — хотя бы подпись у префилла, что это нехватка под расчётный объём. Если в Ф24 побеждает вариант (А), вопрос сужается до префилла количества в модалке склада.
- [ ] **Ф27 (P2, полировка). «liquid» не переведено** на карточках склада и в модалке добавления («liquid · Атт. 74% · 20-22°C»), при этом деталка каталога пишет по-русски «Форма: жидкие». Единый словарь формы дрожжей (сухие/жидкие) во всех трёх местах. Кандидат в пакет полировки Ф13–Ф19.

Артефакт осмотра: на складе qa.admin осталась позиция «Хефевайцен Эль» 23.3 мл (добавлена кнопкой «На склад» при проверке); чистка — `npm run seed:qa` или вручную.

---

## Что работает отлично (не трогать)

- **Мастер воды** — сильнейшее место прохода: профили из каталога ранжируются «подходит по стилю», соли и кислота считаются вместе (гипс 0.24 г / CaCl₂ 0.11 г / эпсом 0.04 г / молочная 6.58 мл на осмосе для Пльзеня — правдоподобно после Ф1–Ф12 водного аудита), доменное предупреждение «Объём до кипячения не помещается в котёл (28,0 л)».
- **Гид варочного дня**: «СЕЙЧАС/Следом», таймеры шагов, «Вернуть шаг», хмель «за 60/10 мин до конца», прогресс 9/9 → «Поставить на брожение».
- **Склад**: списание с историей движений и «Вернуть на склад» (Цитра −120 г корректно сложила три закладки), «Рецепты под ваш склад» с процентом и списком нехваток.
- Диалоги с guardrail'ами: копирование («попадёт как черновик»), запуск нагрева («убедитесь, что в ёмкости есть вода»), завершение («день 1 из 35 … этап можно вернуть»).
- Онбординг-чеклист «С чего начать», виджеты дашборда, автосейв редактора.

## Инфраструктурная заметка (не продуктовые баги)

Во время прогона в системе крутились **три** dev-сервера (два посторонних, без TTY — вероятно, от параллельной сессии; см. memory `parallel-sessions-worktrees`). Они делили `.next` и давали каскад артефактов: 500/404 на роутах, слетевший CSS (страница «Мои рецепты» рендерилась картинкой стиля BJCP на весь экран — это НЕ продуктовый баг), потеря куки → dev-автологин молча подменял qa.user на qa.admin (memory `dev-anon-qa-trap`), из-за чего пикер склада разово показал чужие остатки. Лечение: убить лишние процессы точечно (`kill -TERM <pid>`), `touch apps/web/next.config.ts` для пересборки манифестов. Правило: параллельные сессии — только в worktree.

Артефакты прогона в БД (qa.user): рецепты «Pilsner Urquell (копия)», «Цитровый IPA»; партии №1 обоих рецептов; профиль Grainfather G30; демо-пивоварня; списанный склад (−5 кг pale ale, −120 г Citra, −11 г US-05). Чистка: `npm run seed:qa`.

## Единый порядок работ (объединён с notes/public-recipe-stock-shopping-links.md)

Пункты П1–П4 из ТЗ «Публичный рецепт ↔ склад и список покупок» вплетены в волны так, чтобы работы в одной зоне кода шли вместе и не конфликтовали:

0. **Волна 0 — выполнено 2026-07-15**: редизайн публичной страницы рецепта закоммичен (полоса показателей с треками стиля) — база для Ф1/Ф6/П1.
1. **Волна 1 (P0, доверие)**: **Ф1** — дрейф статов копии, принятый вариант (а)+(б): копия наследует эффективность/объём/параметры оборудования оригинала; баннер «показатели пересчитаны, ABV X → Y» с кнопкой «вернуть параметры автора» показывается только когда пользователь потом сам меняет профиль/параметры.
2. **Волна 2 (P0, склад и завершение)**: **Ф2 + П3 — СДЕЛАНО 2026-07-15** (замены при списании + выходы для нехваток без привязки; бонус-фиксы по ревью: лот из `inventorySelectionMeta` в превью, чужой `userCustomIngredientId` зануляется в match-DTO, ref-guard префилла кастомной формы). **Ф3 — СДЕЛАНО 2026-07-15** (лексика «партия» у действий + вердикты batch_completed/fg_confirmed). Волна закрыта.
3. **Волна 3 (P0/P1, точечная механика) — ЗАКРЫТА 2026-07-15**: Ф4 (7235c59), Ф5 (7b4696c), Ф10 (31d4b19), Ф9 («граммы как факт», см. пункт выше). Факт разведки: продуктовой граммовки пачек в каталоге 0/349 дрожжей, работает эвристика «dry → 11 г» (35% каталога). **Отдельный тикет (решение владельца)**: снять гейт `showPackageVariants === "consumable"` в admin-ingredient-form для дрожжей + наполнить реальные граммовки топ-штаммов через `ingredient_package_variants` — тогда скобка «(N пачек)» станет точной и массовой.
4. **Волна 4 (P1, публичная страница как хаб) — ВЫПОЛНЕНА 2026-07-15** (3 коммита: 7228e5b Ф6+П1+П2, ba1658c Ф8, 9a75188 Ф7+Ф11). Формулировки согласованы с владельцем ДО правок: Ф6 — «В закладки» + раздел «Закладки»; тост П2 — без пояснений, при нехватках title «В закладках» + кнопка «Чего не хватает» → /app/shopping, иначе кнопка «Закладки» → /app/saved; Ф7 — полный вариант с автооткрытием; Ф8 — транслит+стиль, раскладка фолбэком, без fuzzy; П1 — плашка-вердикт после «Ключевых показателей» (lg:hidden), общий клиентский контекст матча (RecipeMatchProvider: плашка/панель/тост не расходятся), скролл-якорь к панели. Процесс: 3 исполнителя → мультиагентное ревью (11 находок, все подтверждены адверсариально, среди них P0 потеря статичности /recipes/[slug]) → 8 фиксов → живой прогон в Playwright (9/9 PASS после фикса регрессии «и»→«i» в поиске). Полный vitest 3386/3386, tsc чистый.
5. **Волна 4.5 (панель матча ↔ склад, по находкам владельца после волны 4)**: **Ф22** (данные сидов — можно делать сразу, ни с чем не конфликтует; выбор аналогов согласовать), затем **Ф23+Ф24+Ф25** одной пачкой (общие файлы recipe-match-panel/match-service; развилку Ф24 А/Б и семантику счётчика Ф25 согласовать с владельцем ДО правок), Ф26 — вместе с Ф24.
6. **Волна 5 (P1/P2)**: Ф12 (демо-сим), Ф20, Ф21, затем пакет полировки Ф13–Ф19 + Ф27 одним проходом. **П4** (индикаторы наличия на карточках ингредиентов) — опционально, только после П1–П2 и просмотра вживую; если шумит — не делать.

Правила совместимости: публичную страницу рецепта (`public-recipe-page.tsx`, `recipe-stats-summary.tsx`, `recipe-match-panel.tsx`, `recipe-save-button.tsx`) трогают только волны 4–5 — после того как Ф1 зафиксирует контракт «копия = статы автора»; Ф2/П3 меняют серверную механику матча/списания, не трогая раскладку страницы.
