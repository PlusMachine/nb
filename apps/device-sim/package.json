{
  "name": "@nb/device-sim",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Lightweight TS simulator of a BrewForge controller speaking the frozen @nb/brewforge-protocol (no hardware needed).",
  "scripts": {
    "dev": "tsx src/main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nb/brewforge-protocol": "workspace:*",
    "@nb/brewforge-sim": "workspace:*",
    "mqtt": "^5.10.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13"
  }
}
