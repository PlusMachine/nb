{
  "name": "@nb/content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "bjcp:sync-images": "node ./scripts/sync-bjcp-images.mjs",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "TMPDIR=/tmp TEMP=/tmp TMP=/tmp vitest run --passWithNoTests"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
