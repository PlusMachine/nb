{
  "name": "@nb/push",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/web-push": "^3.6.4",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@nb/brewforge-protocol": "workspace:*",
    "@nb/db": "workspace:*",
    "web-push": "^3.6.7"
  }
}
